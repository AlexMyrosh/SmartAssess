@model Presentation_Layer.ViewModels.UserManagement.AllUsersViewModel

<div id="userList">
    <div class="row">
        @Html.Partial("PartialViews/_UserList", Model)
    </div>
</div>

<div class="d-flex justify-content-center" id="usersPaginationContainer" style="font-size: larger;">
    @Html.Partial("PartialViews/_Pagination", Model.Pagination)
</div>